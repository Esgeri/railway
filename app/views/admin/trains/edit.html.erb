<h1 class="grid-bottom"><%= t('.title') %></h1>

<%= render 'form', train: @train %>

<%= link_to t('common.show'), [:admin, @train], class: 'btn btn-primary' %>
<%= link_to t('common.back'), admin_trains_path, class: 'btn btn-primary' %>
