<tr>
  <td><%= route.name %></td>
    <td>
      <%= link_to t('common.show'), [ :admin, route ] %>
      <%= link_to t('common.edit'), edit_admin_route_path(route) %>
      <%= link_to t('common.destroy'), [ :admin, route ], method: :delete, data: { confirm: t('common.confirm') } %>
    </td>
</tr>
