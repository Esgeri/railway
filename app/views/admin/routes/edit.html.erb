<h1 class="grid-bottom"><%= t('.title') %></h1>

<%= render 'form', route: @route %>

<%= link_to t('common.show'), [ :admin, @route ], class: 'btn btn-primary' %>
<%= link_to t('common.back'), admin_routes_path, class: 'btn btn-primary' %>
