<p>
  <strong>Station title:</strong>
  <%= @railway_station.title %>
</p>

<% if @railway_station.routes.empty? %>
  <p>The station is not included in any routes</p>
<% else %>
  <p>The station enters the following routes:</p>
  <ul>
    <% @railway_station.routes.each do |route| %>
      <li><%= link_to route.name, [ :admin, route ] %></li>
    <% end %>
  </ul>
<% end %>

<p><strong>Trains on railway station:</strong></p>

<% if @railway_station.trains.present? %>
  <ul>
    <% @railway_station.trains.each do |train| %>
      <li><%= link_to train.number, [ :admin, train ] %></li>
    <% end %>
  </ul>
<% else %>
  No trains on current station
<% end %>



<%= link_to 'Edit', edit_admin_railway_station_path(@railway_station) %> |
<%= link_to 'Back', admin_railway_stations_path %>
